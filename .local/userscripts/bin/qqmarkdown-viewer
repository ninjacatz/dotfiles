#!/bin/bash
source_dir=$(cd -- "$(dirname -- "$0")" && pwd)
. "$source_dir/../qquser-check"

if ! [[ $1 == *.md || $1 == *.MD ]]; then
	echo "Invalid argument (.md or .MD file)"
	exit 1
fi

pandoc $1 > $1.html
dillo $1.html
rm $1.html
