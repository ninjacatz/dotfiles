#!/bin/bash
set -eo pipefail
source_dir=$(cd -- "$(dirname -- "$0")" && pwd)
. "$source_dir/../qquser-check"

calendar_path="/mnt/nfs/calendar"

"$source_dir/qqnfs" -mount hs
calcurse -D "$calendar_path"
